# Meshtastic Pager Mode Fork

![Pager Mode banner](assets/pager-mode-banner.svg)

> Неофициальный форк Meshtastic с доработками pager mode для маленьких экранов и e-ink устройств.

## О проекте

Это **форк** Meshtastic, а не официальный upstream-релиз.

Это community fork без коммерческой поддержки и без финансовой выгоды.

Значительная часть работы в этом форке была сделана с помощью **искусственного интеллекта**. Это указано специально и открыто. Автор проекта работает в другой области, поэтому AI здесь использовался как практический инструмент для разработки и документации.

## Язык

- English: [README.md](README.md)
- Русский: этот файл

## Что меняет этот форк

- Добавляет `Pager Mode` в классический small-screen UI
- Переносит ту же идею в `InkHUD` для поддерживаемых e-ink устройств
- Фиксирует устройство на выбранном DM или канале, пока активен pager mode
- Открывает действия сообщения длинным нажатием, включая включение и выключение pager mode
- После перезагрузки возвращает устройство на обычный стартовый экран, а не восстанавливает pager mode автоматически
- Делает длинные сообщения более читаемыми
- Старается оставить изменения меньше, чем полный редизайн интерфейса

## Для каких устройств это сделано

Форк в первую очередь рассчитан на маленькие экраны:

- OLED-устройства Meshtastic
- e-ink устройства на `InkHUD`

Большие TFT-устройства здесь не были основной целью.

## Область проекта

- Сфокусировать интерфейс на чтении сообщений
- Не раздувать изменения больше необходимого
- Сохранить как можно больше поведения upstream
- Упростить локальную сборку, упаковку и прошивку для используемых плат

## Рекомендуемые цели сборки

Примеры:

```bash
pio run -e heltec-v3
pio run -e heltec-v4
pio run -e heltec-wireless-paper-inkhud
pio run -e t-echo-inkhud
```

Используйте тот `env`, который соответствует вашему устройству.

## Сборка, упаковка, прошивка

Локальная папка для собранных и разложенных артефактов:

- [../../release-work/README.md](../../release-work/README.md)

## Основной путь для пользователя

Сейчас самый простой вариант для большинства пользователей такой:

1. Открыть [Pager Mode Web Flasher](https://chemobook.github.io/meshtastic-pager-mode/).
2. Использовать настольный Chrome или Edge.
3. Выбрать `Heltec V3` или `Heltec V4`.
4. Прошить устройство прямо из браузера и дождаться перезагрузки.
5. Если serial-порт не появляется, сначала замени USB-кабель и закрой другие serial/terminal-программы.
6. Если Windows всё ещё не видит плату, установи драйвер ESP32 USB serial по [официальной инструкции Meshtastic](https://meshtastic.org/docs/getting-started/serial-drivers/esp32/).

Пока через веб-страницу доступны только эти две платы.

Чтобы упаковать актуальные сборки в эту папку:

```bash
./bin/pager-package.sh heltec-v3 heltec-v4
```

Скрипт копирует свежие артефакты из `.pio/build/<env>/` в удобные release-папки и при необходимости подтягивает OTA-образ для ESP32-S3.

### Быстрый путь

```bash
pio run -e heltec-v3
pio run -e heltec-v4
```

### Упаковать результат

```bash
./bin/pager-package.sh heltec-v3 heltec-v4
```

### Прошить через форковый helper

```bash
./bin/pager-flash.sh --board heltec-v3 --port /dev/tty.usbmodemXXXX
./bin/pager-flash.sh --board heltec-v4 --port /dev/tty.usbmodemXXXX
```

Этот wrapper использует существующий `bin/device-install.sh`, но делает выбор готовых файлов для `Heltec V3/V4` заметно проще.

## Практические замечания

- Перед прошивкой важно точно проверить соответствие `env` и платы.
- Для обычной пользовательской прошивки `heltec-v3` и `heltec-v4` лучше использовать веб-прошивальщик.
- Браузерная прошивка зависит от Web Serial, поэтому Safari и большинство мобильных браузеров здесь не основной путь.
- Поведение pager mode лучше проверять на реальном устройстве, а не только по сборке.
- Эту ветку стоит воспринимать как форк с точечными изменениями, а не как полную замену upstream Meshtastic.

## Ограничения

- Это всё ещё community fork, поэтому проверка на реальном железе важна.
- Большие TFT-цели не были основным фокусом.
- Русские дефолты здесь практичны, но они заметно более специфичны, чем upstream.
- Главная цель этого форка — польза, а не абсолютное покрытие всех плат.
